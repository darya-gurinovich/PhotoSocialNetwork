@model List<PhotoSocialNetwork.ViewModels.Logs.RegistrationLogViewModel>

<div id="userBlockingLogsContainer">
    @if (Model.Count == 0)
    {
        <label>No logs were found</label>
    }
    @foreach (var log in Model)
    {
        <div style="background: #ddd; margin: 10px">
            <p>User: @log.UserName</p>
            <p>Registration Date: @log.RegistrationDate</p>
            <p>Phone: @log.Phone</p>
        </div>
    }
</div>